#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../jsinspect/bin/jsinspect" "$@"
  ret=$?
else 
  node  "$basedir/../jsinspect/bin/jsinspect" "$@"
  ret=$?
fi
exit $ret
